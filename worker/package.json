{
  "name": "visitor-map-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute visitor-map --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute visitor-map --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5",
    "wrangler": "^3.99.0"
  }
}
